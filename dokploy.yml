app_name: app
app_dir: ./
build:
  command: "npm_config_production=false pnpm install --frozen-lockfile && pnpm run build"
deploy:
  command: "pnpm start"
domain:
  - codeinnova.es
  - www.codeinnova.es