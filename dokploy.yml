app_name: app              # Nombre de tu aplicación
app_dir: ./                   # Carpeta raíz donde está package.json
build:
  command: "pnpm install && pnpm run build"  # Instala dependencias y construye la app
  env:
    NODE_ENV: production      # Variable de entorno para producción
deploy:
  command: "pnpm start"        # Comando para iniciar la app
domain:
  - codeinnova.es
  - www.codeinnova.es