app_name: app
app_dir: ./
build:
  command: "pnpm store prune && pnpm install --frozen-lockfile && pnpm run build -- --no-cache"
deploy:
  command: "pnpm start"
domain:
  - codeinnova.es
  - www.codeinnova.es