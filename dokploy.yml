app_name: app
app_dir: ./
build:
  image: node:20.11
  cache:
    - path: ./node_modules
    - path: ./.next/cache
  commands:
    - pnpm install --frozen-lockfile
    - pnpm build
deploy:
  command: "pnpm start"
domain:
  - codeinnova.es
  - www.codeinnova.es